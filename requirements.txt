# Everything the course notebooks import beyond the Python standard library.
# Kept deliberately small: this course teaches core Python, not a stack.
pandas>=2.2,<3
matplotlib>=3.9
numpy>=2.0

# Needed only to run tools/check_materials.py, not by students.
nbclient>=0.10
nbformat>=5.10
ipykernel>=6.29
