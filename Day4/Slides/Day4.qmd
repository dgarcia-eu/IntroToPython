---
title: "Introduction to Programming<br>with Python<br><span class='session'>Day 4 &ndash; Quiz on Day 3</span>"
author: "David Garcia<br><span class='semester'>Winter Semester 2026-27</span>"
format:
  revealjs:
    theme: [default, ../../assets/unikn.scss, ../../assets/introtopython.scss]
    width: 1280
    height: 720
    margin: 0.06
    center: false
    slide-number: false
    progress: true
    controls: true
    history: true
    logo: ../../assets/unikn-logo.png
    footer: "<span class='fbar-left'>Day 4</span><span class='fbar-mid'>Introduction to Programming with Python</span><span class='fbar-right'>Winter Semester 2026-27</span>"
    highlight-style: github
    link-external-newwindow: true
    fig-align: left
---

## Recap of Day 3

*Fill in from yesterday's feedback form before the session. Delete the slide if
there is genuinely nothing to pick up — but check the form first.*

::: {.incremental}
- **You asked us to repeat:** …
- **We ran out of time on:** …
- **The assignment tripped people up on:** …
:::

**Anything else from yesterday you want to go over? Now is the moment.**


## Quiz I


1. Assume you have the following dictionary:

```python
dictionary = { "applez": 100, "oranges": 120, "bananas": 50}
```
How can I change the key of the key-value pair ```("apples", 100)```?


2. What will be the output of the following code?

```python
num_sum = 10

def doubler():
    num_sum *= 2
    
doubler()
```

---

3. What will be the output of the following code?

```python
def initialize():
    first_value = 1 

print(first_value)
```

4. What is the correct output of the following code? 

```python
def text_to_list(text):
  t = text.lower()
  return t.split()

some_text = "The Old Man in The Sea"
print(text_to_list(some_text))
```

1. ['the', 'old', 'man', 'in', 'the', 'sea']
2. ['The', 'Old', 'Man', 'in', 'The', 'Sea']
3. ["the, old, man, in, the, sea"]
4. "the, old, man, in, the, sea"

---

5.  Write the output of the following code:

```python
word = 'letters'
letters_d = {x: word.count(x) for x in word}
print(letters_d['e'])
```

6.  Describe the role of lambda in this code:

```python
countpairs = [(x,y) for (x,y) in counts.items()]
countpairs.sort(key=lambda x: x[1], reverse = True)
```

---


## Feedback on Day 3

![](QR.png){height="12.5em" fig-align="center"}

